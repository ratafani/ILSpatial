import SwiftUI

public enum ColorTokens {
    public static let primaryText = Color.primary
    public static let secondaryText = Color.secondary
    public static let accent = Color.blue
    public static let background = Color.black.opacity(0.8)
}

public enum TypographyTokens {
    public static let header = Font.title.bold()
    public static let subheader = Font.headline
    public static let label = Font.caption
}
