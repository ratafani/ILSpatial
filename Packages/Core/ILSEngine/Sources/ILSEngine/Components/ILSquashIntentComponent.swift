import RealityKit

/// Attach this component to an entity that possesses an `ILSquashAnimationComponent` to trigger the animation.
public struct ILSquashIntentComponent: Component {
    public init() {}
}
